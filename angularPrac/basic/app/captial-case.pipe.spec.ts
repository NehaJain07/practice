import { CaptialCasePipe } from './captial-case.pipe';

describe('CaptialCasePipe', () => {
  it('create an instance', () => {
    const pipe = new CaptialCasePipe();
    expect(pipe).toBeTruthy();
  });
});
